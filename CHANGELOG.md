# 更新记录

## 0.4.0 — 2026-08-26

- 单词 AI 发音由 Melo 更换为 Piper 高质量模型：美音 `en_US-lessac-high`、英音 `en_GB-cori-high`；成品随带模型、配置、模型卡、GPL 文本和 eSpeak 数据，并校验资源摘要及固定的 22.05 kHz 输出。
- 新增并持久化统一“自动发音偏好”，旧设置默认迁移为“优先速度”；US/UK 单双击、双击 Alt 自动朗读、短语/句子及中文英译朗读使用同一映射，并在切换偏好时取消旧音频。
- 按钮按下即建立当前双击窗口专用的可取消 AI 候选；双击取消同轮单击，速度/自然模式分别提升或丢弃候选，不会出现 SAPI/AI 叠音、串音或迟到补播。
- Piper/Kokoro 启动预热执行真实小样本推理，并向按钮候选和最新正式 AI 请求协作让路；移除旧选区双口音缓存，SAPI 继续使用独立 COM 线程。
- 删除超过 45 字符改走 SAPI 的临时规则。Kokoro 长句只在自然语义停顿处分段，并依据真实生成耗时与音频时长，在至少两段、约 5 秒缓冲且预测安全时提前播放；否则等待整句完成。新增结构化时序日志与分段、缓冲、抢占和取消回归测试。
- 文档统一为微信取词已取消、Piper 固定 22.05 kHz；旧版 0.3.2 的 Melo/微信记录仅保留为历史。
- 构建流程改用隔离的 Python 3.12 环境，避免把全局环境中的 Gradio、数据分析或模型训练依赖带入成品；ZIP 和烟测通过后清理重复构建目录与旧候选。
- 引擎与桌面生命周期测试把运行日志重定向到临时目录，避免模拟故障写入真实用户的 `app.log`；完整测试结束后临时日志自动清理。

## 0.3.2 — 2026-08-11

- 修正 Windows 发布构建：排除仅用于模型转换或数据分析的开发依赖，避免把无关组件带入用户下载包。

## 0.3.2-test.3-hotfix.5 — 未发布测试版

- 修复双击 Alt 重新获取当前选区时迷你窗跟随当前鼠标位置跳动；同一来源窗口现在保留原选词锚点，首次取词或切换来源窗口仍使用新的安全位置。

## 0.3.2-test.3-hotfix.4 — 未发布测试版

- 修复 CrispASR `v0.8.28` 的 Melo session 已返回 24 kHz PCM、采样率 getter 却错误报告 44.1 kHz，导致单词播放被加速约 1.84 倍并升调的问题；成品冒烟现在强制校验 Melo WAV 为 24 kHz。
- 移除会让短语音出现重复、挤压和金属感的自制后处理变速；Melo 单词固定保持模型原版语速，设置中的三档只使用 Kokoro 自身的句子级原生速度参数。
- 把真人听感与技术检查分开：WAV 可解析、时长正确和自动测试通过不再代表发音自然，测试包仍须由真人实际试听后才能完成感官验收。

## 0.3.2-test.3-hotfix.3 — 未发布测试版

- 新版发音在后台预热模型并将刚翻译出的美音短暂缓存；点击播放时复用已合成音频。
- Kokoro CPU 推理限制为四线程，避免短单词被全机器默认线程池的调度开销拖慢。

## 0.3.2-test.3-hotfix.2 — 未发布测试版

- 新版本地神经发音增加“慢 / 标准 / 快”三档；旧有速度是“快”，默认改为“标准”。
- 设置立即保存；切换速度会取消旧的神经语音请求，避免新旧速度串音。

## 0.3.2-test.3-hotfix.1 — 未发布测试版

- 取词热路径与迷你窗定位回到 `0.3.2-test.3` 行为，取消微信（`wechat.exe` / `weixin.exe`）取词支持。
- 迷你窗底部使用“复制译文”，设置继续从大窗口或托盘进入。
- 迷你窗可见期间保持不激活，兼容读取还原剪贴板前再次确认用户复制意图，避免划词后 `Ctrl+C` 被浮窗或后台还原干扰。

## 0.3.2-test.4 — 未发布测试版

- 对抗式审查扩展为刁难用户场景：覆盖快速连续换词、翻译途中关闭/暂停、物理修饰键、剪贴板并发、损坏设置、托盘失败、跨屏边缘、发音抢占和安装中断，不再只审代码。
- 修复设置中直接用键盘录入“双击 Alt”时第二次松键触发异常。
- 迷你窗按鼠标所在显示器工作区定位，支持负坐标副屏、不同方向任务栏和狭小工作区；大窗口与设置页首次显示也限制在当前工作区。
- 新选区立即使旧翻译失效；用户已关闭浮窗、暂停取词或禁用当前应用后，迟到结果不会让浮窗复活或继续发音。
- 软件自身窗口内点击、键入、标题栏操作和迷你/大窗切换不再误杀在途翻译；真实外部新操作仍会使旧任务失效。
- 取词失效与结果显示统一排入 UI 主线程并二次复核代次，消除 A 刚通过检查、B 已开始后 A 又回显的竞态。
- 快速 A→B→C 加入 45ms 最新请求合并，保持模型最大并发为 1 并跳过尚未开始的 B；删除会吞掉真实重复选词的 0.7 秒同文去重。
- 新选词、暂停或禁用当前应用时同步清空旧原文、译文、元信息和待处理队列，避免“B 原文配 A 译文”及永久加载。
- SAPI 发音统一到单一 COM 工作线程，新请求清除旧朗读；取消发音失败会重建语音对象，不会杀死后续发音能力。
- 关闭窗口或点击窗外会停止长句发音；仅切换迷你/大窗口不会误停。
- 设置页录入双击 Alt 与运行时使用相同的 Ctrl、组合键和滚轮中断规则；所有兼容复制在准备阶段和真正注入瞬间都复核物理修饰键。
- UIA 安全判断改为安全、密码、未知三态；密码、超时、扫描不完整或未知状态都禁止兼容复制，微信无法安全确认时明确失败而不冒险读取。
- 修复 ECDICT 字面换行、陈旧缓存和缺少中文释义时的模型回退；SQLite 连接在 Windows 上显式关闭。
- 设置损坏时备份原文件并关闭桌面取词安全启动；有效临时文件可恢复，保存失败会回滚界面和内存状态。
- 单实例从固定端口改为 Win32 命名互斥锁；托盘失败时保留可见大窗口与错误提示。
- 成品冒烟检查新增 SQLite 全库完整性与真实词条查询，损坏但体积正常的词典不能进入安装接管。
- 安装标记改为 Windows PowerShell 5.1 兼容的同目录原子替换，并可从 pending/backup 恢复；复制前先落未通过标记，强杀留下的 staging 下次可安全识别。
- 卸载先把正式目录同卷隔离，完整删除后才移除快捷方式；两个用户数据目录先全部预检，未知文件、联接或中途失败都会保留明确残留路径。

## 0.3.2-test.3 — 未发布测试版

- 默认重新获取操作改回连续轻按两次左 `Alt`；读取成功后自动播放美音，中文等待对应英译完成后再播放。
- 新增可配置快捷键：重新获取与窗口切换都可在设置中直接录入；占用或无效组合不会覆盖原设置。
- 双击 Alt 手势加入误触隔离：单按、长按、自动重复、`Alt+Tab`、`Alt+C`、Ctrl+Alt、右 Alt/AltGr 及两次点击间的其他输入均不会触发。
- 修复微信在原剪贴板含 Chromium 内部来源元数据时提前跳过兼容复制的问题；只精确放行可完整重建的 source URL 与 RFH token HGLOBAL 字节。
- 新增双击 Alt 竞态、快捷键回滚、Chromium 精确白名单与逐字节还原测试。
- 新增 Excel 单元格、多单元格与多区域 COM 选区读取，并校验前台工作簿窗口和选区稳定性。
- Word COM 读取增加前台窗口身份和选区稳定性校验，避免多实例时误读其他文档。
- 经典 Outlook 的独立邮件窗口、内嵌回复和普通阅读窗新增 WordEditor 选区读取；新版 Outlook 安全回退到无障碍读取。
- 为 Office、WPS、浏览器内置 PDF 与桌面 PDF 阅读器等复杂应用增加有界深层 UIA 选区探测；只读当前选区，不读取整篇文档。
- 默认常用应用补齐 Word、Excel、PowerPoint、新旧 Outlook、OneNote，WPS 文字/表格/演示/PDF，Edge、Chrome、Firefox，Adobe Reader/Acrobat、Foxit，微信、QQ 和记事本。
- 自动显示大窗口改为不激活源窗口，避免划词后原应用的 `Ctrl+C` 失效；用户继续操作后出现的剪贴板变化一律保留。
- 迷你窗底部“设置”改为“复制译文”，设置继续从大窗口或托盘进入。
- 新增需求总账、PDF 深层读取、Word/Excel 前台校验、快捷键事务与焦点保护回归测试。
- 安装升级改为互斥、空间预检、完整运行契约校验和临时用户目录离线冒烟均通过后再原子接管；异常中断可识别并安全恢复本项目标记的 staging/backup。
- 安装与卸载遇到未知顶层文件会明确列出并拒绝删除；只停止当前用户从正式安装路径启动的实例，不影响便携版或测试副本。
- 卸载脚本现在随程序安装并创建开始菜单卸载入口；程序目录删除失败时不会提前移除快捷方式。

## 0.3.2-test.2 — 未发布测试版

- 将手动重试改为 `Alt+X`：重新获取成功后自动播放美音；中文等待对应英译完成后再播放。
- 新增 `Alt+C` 在迷你浮窗与大窗口之间切换，热键显示大窗口时不主动抢焦点。
- 修复长句挤掉迷你窗 US/UK/关闭按钮，并加固大窗口发音按钮列。
- 微信兼容读取改为只保存受支持且可完整重建的托管 Win32 格式，移除裸 OLE 指针方案。
- 微信多进程复制按完整可执行文件路径核验，避免同一安装目录下的复制进程被误判。
- 取词、翻译与自动发音均绑定请求代次，旧结果不会为新选区误发音。
- 新增原生热键、剪贴板恢复、长句布局、竞态与自动发音隔离测试。

## 0.3.1 — 2026-07-21

- 修复 PowerPoint 选中文字无法送入翻译窗口：新增 `ppSelectionText` 专用 COM 读取。
- 修复微信等自绘应用兼容取词等待过短的问题，并在文字格式稍晚出现时继续轮询。
- 修复 UI Automation 因控件矩形相同或矩形读取异常而漏掉有效选区的问题。
- 当时曾改用 OLE 快照扩大剪贴板兼容范围；该方案已在后续测试版移除，当前只处理受支持且可完整重建的托管格式。
- 新增焦点、鼠标交互和剪贴板并发校验，避免慢速取词覆盖用户随后复制的新内容。
- 新增 27 项隔离回归测试；测试不会操作真实桌面应用或系统剪贴板。

## 0.3.0 — 2026-07-21

- 产品正式更名为“大声发划词翻译”，作者署名“眼泪斷了线”。
- 统一窗口、托盘、EXE、安装目录、快捷方式和发布包名称。
- 新增按应用启用、常用与其他应用分组、批量开关和自定义常用应用。
- 新增纯白现代界面、迷你浮窗与完整大窗口两种模式。
- 迷你浮窗按内容和 Windows DPI 动态调整高度，避免底部按钮被裁切。
- 新增托盘菜单、点浮窗外关闭、密码框保护和剪贴板保护。
- 保留离线 ECDICT、OPUS/CTranslate2 中英翻译和 Windows SAPI 英美发音。
- 首次启动时可从 0.2.x 旧目录复制设置和翻译缓存。
