# 安全策略

## 支持范围

安全修复优先面向最新 GitHub Release。旧版本建议先升级后复现。

## 报告安全问题

请使用 GitHub 仓库的 **Private vulnerability reporting** 提交，不要在公开 Issue 中披露可利用细节、个人文档内容、剪贴板内容或凭据。

报告建议包含：

- 受影响版本和 Windows 版本；
- 最小复现步骤；
- 预期行为与实际行为；
- 是否涉及密码框、剪贴板、应用白名单或本地数据目录；
- 已脱敏的日志片段。

本项目不会要求报告者提供真实密码、API Key 或私人文档。

## 本地安全边界

- 选区读取默认离线；密码控件以及无法明确判断安全性的 UI Automation 状态会停止兼容复制。
- 剪贴板兼容读取只接受可完整重建的白名单格式，遇到未知 OLE/GDI 句柄、虚拟附件、超限内容或用户并发复制时取消。
- 安装与卸载只处理通过路径、产品标记和运行契约校验的目录；未知顶层文件会阻止自动删除。
